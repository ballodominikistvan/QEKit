version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    rust: "stable" # Installs Rust toolchain on the RTD builder

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs # Installs Sphinx and themes defined in pyproject.toml